//let (can be accessed within the block) and const

//arrow function
//use after initialization

//default parameters
const greet = (firstname, middlename, lastname =" ") => {
    return `Hello, ${firstname} ${middlename} ${lastname}`;
}
console.log(greet("ajn", "snd"));
